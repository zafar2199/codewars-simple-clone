import UserProfile from "./components/UserProfile";

const Home = () => {
    return (
        <div className="home__container">
            <UserProfile />
        </div>
    );
};

export default Home;
